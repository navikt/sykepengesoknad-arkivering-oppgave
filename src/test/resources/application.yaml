app.name: syfogsak
fasit.environment.name: local
server.port: 8081

management:
  endpoint.prometheus.enabled: true
  endpoints.web:
    base-path: /internal
    exposure.include: prometheus
    path-mapping.prometheus: /prometheus

srvsyfogsak:
  username: username
  password: password

javax.net.ssl:
  trustStore: truststore
  trustStorePassword: truststorepassword

servicegateway.url: servicegateway.url
securitytokenservice.url: sts.url

behandleJournal.v2.endpointurl: "behandleJournalv2.url"
Journal.v2.endpointurl: "behandleJournalv2.url"
aktoer.v2.endpointurl: "aktor.url"

virksomhet:
  behandlesak.v1.endpointurl: "behandlesakv1.url"
  arbeidsfordeling.v1.endpointurl: "arbeidsfordeling.url"
  oppgavebehandling.v3.endpointurl: "oppgavebehandling.url"
  oppgave.v3.endpointurl: "oppgave.url"
  person.v3.endpointurl: "person.url"

mockWS: true

spring:
  jackson.serialization.WRITE_DATES_AS_TIMESTAMPS: false
  h2.console:
    enabled: true
    path: '/h2'
  datasource:
    url: 'jdbc:h2:mem:testdb'
    driver-class-name: org.h2.Driver
    username: 'sa'
    password:
  jta.atomikos:
    datasource:
      max-pool-size: 32
      min-pool-size: 8
    properties:
      log-base-dir: transaction-logs/test

  embedded.kafka.brokers: 'localhost:3333'
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    consumer:
      group-id: syfogsak-local
      max-poll-records: 1
      auto-offset-reset: latest
      enable-auto-commit: false

unleash-api:
  url: http://unleash-api/api/

toggle:
  config:
    SKAL_LESE_SOKNADER_FRA_KOE:
      enabled: true
      byEnvironment:
      - local
